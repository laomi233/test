/* Wrapper 样式：让 AEM 蓝框好看点 */
.aem-table-container-wrapper {
    background-color: transparent;
    transition: border 0.3s;
}
.aem-table-container-wrapper:hover {
    border-color: #ddd; /* 鼠标划过显示虚框，提示用户这里是容器 */
}

/* 一个小的 label，提示用户点哪里可以选中组件 */
.aem-wrapper-label {
    font-size: 10px;
    color: #ccc;
    text-transform: uppercase;
    margin-bottom: 5px;
    user-select: none;
}

/* 触发编辑的按钮 */
.btn-trigger-edit {
    position: absolute;
    top: 5px;
    right: 5px;
    z-index: 100; /* 高于 AEM Overlay */
    padding: 5px 10px;
    background: #007bff;
    color: white;
    border: none;
    border-radius: 3px;
    cursor: pointer;
}

/* 纯表格样式 */
.pure-table-editor {
    background: white;
    border: 2px solid #007bff;
    padding: 10px;
    position: relative;
    z-index: 200; /* 编辑时覆盖一切 */
}

.hsi-table { width: 100%; border-collapse: collapse; }
.hsi-table th { background: #5a5a5a; color: white; padding: 10px; }
.hsi-table td { background: #f7f7e8; color: #333; padding: 10px; border-bottom: 1px solid #ddd; }